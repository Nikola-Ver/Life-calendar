import { type } from 'os';
import './Calendar.css';

interface CalendarProps {
    viewType: number;   
} 

const Calendar: React.FC<CalendarProps> = (props) => {
    return (
      <>
      </>
    );
}

export default Calendar;